# ═══════════════════════════════════════════════════════════
# MedStore Pro — Nginx config for existing ep-nginx container
# 
# OPTION A: If you want to add MedStore under existing Nginx
#   Copy this into your earning platform's nginx conf
#   Add a new server block for the MedStore API subdomain/path
#
# OPTION B: Access API directly via VPS-IP:5001
#   No nginx config needed — just use http://VPS-IP:5001/api
#   This is simpler if you don't have a domain
# ═══════════════════════════════════════════════════════════

# If you have a domain later, add this server block:
# server {
#     listen 80;
#     server_name api.yourdomain.com;
#
#     location / {
#         proxy_pass http://HOST-IP:5001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 90s;
#         client_max_body_size 10M;
#     }
# }
